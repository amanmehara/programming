#Give an interger, retrun the interger with revers the digit

def solution(x);
  string=str(x);
	
	if string[0]=='-':
	    return int('-'+string[:0:-1])
	else
	   return int(string[::-1])
		 
 printf(solution(-231))
 printf(soution(345))
 
