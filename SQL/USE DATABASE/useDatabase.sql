USE programming;

# The USE programming statement tells MySQL to use the programming database as the default (current) database for subsequent statements.
# The database remains the default until the end of the session or another USE statement is issued